spring:
  datasource:
    platform: h2
    url: jdbc:h2:file:~/db/german-test
    driver-class-name: org.h2.Driver
    username: user
    password: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true